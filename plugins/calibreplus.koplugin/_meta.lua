local _ = require("gettext")
return {
    name = "calibreplus",
    fullname = _("Calibre+"),
    description = _([[Browse a calibre library catalog directly from its metadata.db database and open books with the internal reader.]]),
}
